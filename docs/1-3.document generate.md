Read docs/SRS_FINAL.md

Generate 2 documents from SRS_FINAL.md.
Extract ALL entities, APIs, status transitions, business rules, and pages from the SRS.
Do NOT omit anything - every item in SRS must appear in the output documents.

1. specs/ARCHITECTURE.md
   - 개발 전략: Backend First
     - Phase A: API Routes 전체 구현 + curl 테스트
     - Phase B: 프론트엔드 구현
   - System architecture diagram
   - ALL API Routes from SRS (complete list with Request/Response 형식)
   - Storage 구조 (ALL entities from SRS)
   - ALL 데이터 흐름 (every CRUD + status transition + calculation)
   - ALL 비즈니스 규칙 from SRS (validation, constraints, conflicts)
   - ALL 에러 처리 scenarios
   - Mermaid diagrams:
     - 시스템 아키텍처
     - 엔티티 관계도 (ERD) - ALL entities
     - 상태 전이 다이어그램 - ALL status fields
     - 페이지 라우팅

2. specs/COMPONENTS.md
   - Derive ALL components from SRS pages and features
   - For each SRS page: list every UI component needed
   - For each SRS entity: list CRUD components (List, Form, Detail)
   - For each SRS feature: list supporting components
   - Layout, Common, and page-specific components
   - Props definitions for each component
   - State management approach
   - Page routing structure

Cross-check: After generating, verify every SRS section is covered.
Documentation only, no code!
Save each file separately.