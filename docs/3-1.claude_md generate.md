Read all project documents:
- docs/SRS_FINAL.md
- specs/ARCHITECTURE.md
- specs/COMPONENTS.md

Create CLAUDE.md:

CRITICAL: Maximum 120 lines total!

Include:
1. Project overview (학원 관리 시스템, Backend First 전략)
2. Tech stack (Next.js + TypeScript + shadcn/ui + Recharts + date-fns + exceljs + react-hook-form + zod + JSON File)
3. Development strategy:
   - Phase A: API Routes 전체 구현 → curl 테스트 → 응답 확정
   - Phase B: 프론트엔드 구현 → 확정된 API 호출
4. Critical constraints:
   - 저장소: data/db.json (lib/storage.ts 유틸리티)
   - API Routes 59개 (상세는 ARCHITECTURE.md 참조)
   - 엔티티 13개, 상태 전이 6개
   - 비즈니스 규칙: 시간표 충돌검증, 정원→대기자 전환, 휴일 출석 차단, inactive 수납 차단, 삭제 제약, 일할계산
   - 에러 코드 체계 (ARCHITECTURE.md 섹션 9 참조)
5. Key features summary (CRUD 13종, 대시보드 통계, 시간표, 출석, 수납/환불, 보강, 대기자, 급여, 휴일, 통합검색, Excel 내보내기, 백업)
6. Development priorities (Phase A → Phase B 순서)
7. Reference: specs/ARCHITECTURE.md, specs/COMPONENTS.md

Keep it concise.
Refer to ARCHITECTURE.md for API details, data flow, error handling.
Refer to COMPONENTS.md for component structure, props, routing.

Save to: CLAUDE.md

DO NOT implement code, only create documentation.